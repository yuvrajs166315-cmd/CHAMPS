# CHAMPS troubleshooting

The duplicate `Home` declaration has been fixed: the Lucide home icon is now imported as `HomeIcon`, while the Home page component remains named `Home`.

If you already have the old folder running, stop Vite with Ctrl+C, replace the project with this version, then run `npm install` followed by `npm run dev`.
