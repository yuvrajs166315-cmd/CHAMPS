# CHAMPS continuation prompt

This repository is a working CHAMPS mobile-first React/Vite/TypeScript application. Preserve the existing UI and user flows. If a backend is later added, use Supabase/Lovable Cloud for authentication, secure profiles, support_requests, volunteers, connections, activities, RSVPs, messages and notifications. Do not replace the CHAMPS identity with a generic template.
